{
  "name": "<%=moduleName%>",
  "version": "0.10.0",
  "description": "sails generate <%=generatorType %>",
  "main": "Generator.js",
  "scripts": {
    "test": "mocha"
  },
  "keywords": [
    "<%=generatorType%>",
    "generator",
    "sails",
    "generate",
    "plugin"
  ],
  "author": "<%= author %>",
  "license": "MIT",
  "dependencies": {
    "lodash": ">=2.4.x",
    "merge-defaults": ">=0.1.0"
  },
  "devDependencies": {
    "sails-generate": "*",
    "reportback": "*",
    "fs-extra": "*"
  },
  "sailsGenerator": {
    "type": "<%= generatorType %>",
    "behavior": "overrides `sails generate <%= generatorType %>`",
    "sailsVersion": "~0.10.0"
  }
}
